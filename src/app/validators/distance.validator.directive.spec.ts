import { DistanceValidatorDirective } from './distance.validator.directive';


describe('DistanceEndDirective', () => {
  it('should create an instance', () => {
    const directive = new DistanceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
